---
title: isEmpty
description: Check if a value is empty (null, undefined, empty string, array, or object).
---

## Installation

<CodeTabs>
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  
  <TabsContent value="cli">
    ```package-install
    npx shadcn@latest add https://reactusekit.dev/r/is-empty.json
    ```
  </TabsContent>
  
  <TabsContent value="manual">
    Copy and paste the following code into your project.

    <CodePreview registryFilePath="lib/is-empty.ts" location="lib/is-empty.ts" />

    Update the import paths to match your project setup.

  </TabsContent>
</CodeTabs>

## Usage

```tsx
import { isEmpty } from "@/lib/is-empty";

function App() {
  const emptyString = "";
  const emptyArray = [];
  const emptyObject = {};
  const nonEmpty = "hello";

  return (
    <div>
      <p>Empty string: {isEmpty(emptyString) ? "true" : "false"}</p>
      <p>Empty array: {isEmpty(emptyArray) ? "true" : "false"}</p>
      <p>Empty object: {isEmpty(emptyObject) ? "true" : "false"}</p>
      <p>Non-empty string: {isEmpty(nonEmpty) ? "true" : "false"}</p>
    </div>
  );
}
```

## API

### Parameters

| Parameter | Type  | Description        |
| --------- | ----- | ------------------ |
| `value`   | `any` | The value to check |

### Returns

| Type      | Description                                 |
| --------- | ------------------------------------------- |
| `boolean` | True if the value is empty, false otherwise |
