---
title: Get Started
description: Copy battle-tested React hooks and utilities into your project
---

import { Callout } from "fumadocs-ui/components/callout";
import { Card, Cards } from "fumadocs-ui/components/card";

# Get Started

<Callout>
  Copy production-ready React hooks and utilities directly into your project.
  Zero dependencies, just code you own.
</Callout>

## Installation

```bash
# Global installation (recommended)
npm install -g react-usekit

# Or use npx (no installation)
npx react-usekit init
```

## Quick Examples

### Add a React Hook

Add a localStorage hook to your project in 30 seconds:

```bash
cd your-react-project
react-usekit init
# Choose "hooks" → "TypeScript" → select "useLocalStorage" → done
```

Use it immediately:

```tsx
import { useLocalStorage } from "./src/hooks/useLocalStorage";

function App() {
  const [name, setName] = useLocalStorage("username", "");

  return (
    <input
      value={name}
      onChange={(e) => setName(e.target.value)}
      placeholder="Type here (auto-saved to localStorage)"
    />
  );
}
```

### Add a Utility Function

Add utility functions to your project in 30 seconds:

```bash
cd your-react-project
react-usekit init
# Choose "utilities" → "TypeScript" → select "formatCurrency" → done
```

Use it immediately:

```tsx
import { formatCurrency } from "./src/utils/formatCurrency";

function ProductPrice({ price }) {
  return <span>{formatCurrency(price)}</span>; // $1,234.56
}
```

## Available Hooks and Utilities

### React Hooks

- **useLocalStorage** - Persist state in localStorage with automatic sync
- **useToggle** - Boolean state with toggle, set true/false functions
- **useDebounce** - Delay value updates until after specified delay
- **useAsync** - Handle async operations with loading, error states
- **useClickOutside** - Detect clicks outside specific elements
- **useEventListener** - Add/remove event listeners with cleanup
- **useWindowSize** - Track browser window dimensions with resize
- **useInterval** - Set up intervals that automatically clean up

### Utility Functions

- **capitalize** - Make the first letter of a string uppercase
- **formatCurrency** - Format numbers as currency with locale support
- **isEmpty** - Check if values are empty (null, undefined, empty objects/arrays)
- **truncateText** - Truncate text with ellipsis at specified length
- **formatDate** - Format dates with customizable patterns
- **slugify** - Convert strings to URL-friendly slugs
- **generateId** - Generate unique identifiers for keys and IDs
- **deepClone** - Deep clone objects and arrays safely

### Features

- **JavaScript & TypeScript** - Both versions included for every hook and utility
- **Zero Dependencies** - Just copy the code you need, no package.json bloat

## Requirements

- Node.js 18+
- React 16.8+ (for hooks)
- Works with: Create React App, Vite, Next.js, custom Webpack

## Next Steps

<Cards>
  <Card
    title="Hooks Reference"
    href="/docs/hooks"
    description="Complete guide to all React hooks with examples"
  />
  <Card
    title="Utilities Reference"
    href="/docs/utilities"
    description="Complete guide to all utility functions with examples"
  />
</Cards>
