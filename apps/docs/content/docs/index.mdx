---
title: Introduction
description: Instantly scaffold production-ready React hooks and utilities into your project with a single command. No dependencies, no boilerplate—just proven, reusable code.
---

import { Callout } from "fumadocs-ui/components/callout";
import { Card, Cards } from "fumadocs-ui/components/card";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";

## Installation

<Tabs items={["npx (Recommended)", "Global Install"]}>
  <Tab value="npx (Recommended)">
    ```bash npx react-usekit@latest init ``` Use react-usekit without installing
    it globally. Always gets the latest version.
  </Tab>
  <Tab value="Global Install">
    ```bash npm install -g react-usekit@latest react-usekit init ``` Install
    react-usekit globally for repeated use.
  </Tab>
</Tabs>

## Quick Example

Let's add a `useBoolean` hook to your React project:

### Step 1: Run the CLI

```bash
cd your-react-project
npx react-usekit@latest init
```

### Step 2: Follow the Interactive Prompts

```bash
? What would you like to add? › hooks
? Choose language: › TypeScript
? Enter relative directory to place files: › src/hooks
? Select hooks to add: › ✓ useBoolean
✅ Hook 'useBoolean' added to src/hooks/useBoolean.ts
```

### Step 3: Use It in Your Component

```tsx
import { useBoolean } from "./src/hooks/useBoolean";

function ToggleExample() {
  const { value: isVisible, toggle, setTrue, setFalse } = useBoolean(false);

  return (
    <div>
      <button onClick={toggle}>{isVisible ? "Hide" : "Show"} Content</button>

      {isVisible && (
        <div>
          <p>This content is toggleable!</p>
          <button onClick={setFalse}>Hide</button>
        </div>
      )}
    </div>
  );
}
```

## What You Get

### ✅ Production-Ready Code

Every hook and utility is battle-tested and production-ready. No prototypes or experimental code.

### ✅ Zero Dependencies

The code is self-contained with no runtime dependencies. You own the code completely.

### ✅ TypeScript & JavaScript

Full support for both TypeScript and JavaScript with proper types and JSDoc comments.

### ✅ Consistent Patterns

All hooks and utilities follow consistent patterns and best practices.

## Available Hooks & Utilities

<Cards>
  <Card title="🪝 Hooks" href="/docs/hooks/use-boolean">
    10 production-ready React hooks for common use cases like state management,
    side effects, and more.
  </Card>
  <Card title="🛠️ Utilities" href="/docs/utilities/capitalize">
    9 utility functions for common operations like string manipulation,
    validation, and formatting.
  </Card>
</Cards>

## CLI Features

- **Interactive prompts** - Easy-to-use interface with clear options
- **Smart path validation** - Prevents common path-related errors
- **Duplicate detection** - Won't overwrite existing files without confirmation
- **Beautiful output** - Clear progress indicators and success messages
- **Cross-platform** - Works on Windows, macOS, and Linux

## Next Steps

1. **Try it out** - Run `npx react-usekit@latest init` in your project
2. **Browse hooks** - Check out the [useBoolean hook](/docs/hooks/use-boolean)
3. **Explore utilities** - See the [capitalize utility](/docs/utilities/capitalize)
4. **Give feedback** - Found a bug or have suggestions? Open an issue on [GitHub](https://github.com/brijeshkumaryadav16/react-usekit)
